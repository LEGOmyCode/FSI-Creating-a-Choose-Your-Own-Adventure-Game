window.prompt('Do you head left or right?')// Let's start by asking the user a question
let answer = window.prompt('Do you head left or right?')// We can get the answer the user types in by assigning a variable
let firstAnswer = window.prompt('Do you head left or right?')//
console.log(firstAnswer)